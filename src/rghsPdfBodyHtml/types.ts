export type BulletLayoutResult = {
    fontSize: number;
    lineHeight: number;
    fittedColumns: string[][];
};

export type SectionPosition = {
    titleTop: number;
    contentTop: number;
    left: number;
    width: number;
    height: number;
};
